// PLACEHOLDER — fill in with the real GA4 Measurement ID (format "G-XXXXXXXXXX")
// once you've created the property. Leave blank and nothing analytics-related
// renders at all — see Layout.astro, which also only fires this in production
// builds, never during `yarn dev`, so local/testing traffic never counts.
export const GA_MEASUREMENT_ID = "";
